import { getDefaultStore } from "jotai";
import { localSelectedDirAtom } from "../../renderer/src/atom";

const store = getDefaultStore();

async function setupLocalMusic() {
  //
}

export { setupLocalMusic };
