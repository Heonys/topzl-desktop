import { MusicItem } from "@shared/plugin/type";

type Props = {
  isPlaying: boolean;
  musicItem: MusicItem;
};

export const ListItem = ({ isPlaying, musicItem }: Props) => {
  return <div></div>;
};
